import styled from "styled-components";
interface TimerProps {}
interface StyleProps {}
const Timer: React.FC<TimerProps> = (props) => {
  return <Wrap>Timer</Wrap>;
};
export default Timer;
const Wrap = styled.div<StyleProps>``;
